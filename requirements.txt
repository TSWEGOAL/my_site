Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-WTF>=1.2
Flask-Limiter>=3.5
python-dotenv>=1.0
mistune>=3.0
gunicorn>=21.2

# 如果要把数据库换成 Render Postgres / Neon / Supabase，
# 取消下面这行的注释（SQLAlchemy 连 PostgreSQL 需要驱动）
# psycopg[binary]>=3.1
