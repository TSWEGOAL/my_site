Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-WTF>=1.2
Flask-Limiter>=3.5
python-dotenv>=1.0
mistune>=3.0
gunicorn>=21.2

# PostgreSQL 驱动：接 Neon / Render Postgres / Supabase 时必需
# （app.py 读 DATABASE_URL 环境变量切换数据库）
psycopg[binary]>=3.1
